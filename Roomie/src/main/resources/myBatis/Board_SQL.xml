<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE mapper
	PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN"
	"http://mybatis.org/dtd/mybatis-3-mapper.dtd">
	
<mapper namespace="board">

<insert id="boardregister" parameterType="hashmap">
      <selectKey resultType="int" keyProperty="BO_IDX"
         order="BEFORE">
         SELECT BOARD_SEQ.NEXTVAL FROM DUAL
      </selectKey>

      <!-- 이게 된다고? -->
   <![CDATA[
      INSERT INTO BOARD(BO_IDX, BO_MEDIA, BO_DATE, BO_HASH, BO_PLACE, BO_CONT, BO_MEM)
      VALUES(
      #{BO_IDX},
      #{BO_MEDIA},
      SYSDATE,
      #{BO_HASH},
      #{BO_PLACE},
      #{BO_CONT},
      #{BO_MEM}
      )
   ]]>




   </insert>

   <insert id="hashinsert" parameterType="hashmap">
   <![CDATA[
      INSERT INTO HASH(HASH_IDX, HASH_BOARD, HASH_KEYWORD)
      VALUES(
      HASH_SEQ.NEXTVAL,
      #{HASH_BOARD},
      #{HASH_KEYWORD}
      )
   ]]>
   </insert>
	
</mapper>