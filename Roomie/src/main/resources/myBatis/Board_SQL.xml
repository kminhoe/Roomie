<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE mapper
	PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN"
	"http://mybatis.org/dtd/mybatis-3-mapper.dtd">
	
<mapper namespace="board">

<insert id="boardregister" parameterType="hashmap">
	<![CDATA[
		INSERT INTO BOARD(BO_IDX, BO_MEDIA, BO_DATE, BO_HASH, BO_PLACE, BO_CONT)
		VALUES(
		BOARD_SEQ.NEXTVAL,
		#{BO_MEDIA},
		SYSDATE,
		#{BO_HASH},
		#{BO_PLACE},
		#{BO_CONT}
		)
	]]>
	<selectKey resultType="int" keyProperty="BO_IDX" order="AFTER">
		SELECT BO_IDX FROM BOARD
		WHERE BO_HASH = #{BO_HASH}
	</selectKey>
</insert>

<insert id="hashinsert" parameterType="hashmap">
	<![CDATA[
		INSERT INTO SEARCH(HASH_IDX, HASH_BOARD, HASH_KEYWORD)
		VALUES(
		SEARCH_SEQ.NEXTVAL,
		#{HASH_BOARD},
		#{HASH_KEYWORD}
		)
	]]>
</insert>

	
	
	
</mapper>